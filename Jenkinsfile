pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Example: Run a shell command to build your project
                sh 'echo "Building..."'
            }
        }
        stage('Test') {
            steps {
                // Example: Run tests
                sh 'echo "Testing..."'
            }
        }
        stage('Deploy') {
            steps {
                // Example: Deploy your application
                sh 'echo "Deploying..."'
            }
        }
    }
}

